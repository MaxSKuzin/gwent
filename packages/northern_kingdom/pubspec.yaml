name: northern_kingdom
description: A starting point for Dart libraries or applications.
version: 1.0.0
publish_to: none

environment:
  sdk: ^3.10.4

# Add regular dependencies here.
dependencies:
  common_entites:
    path: ../common_entites

dev_dependencies:
  lints: ^6.0.0
  flutter_lints: 6.0.0
